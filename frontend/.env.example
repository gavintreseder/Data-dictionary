# When running the frontend separately from the backend, point it at the API.
# In the Docker/Railway single-container build, leave this blank so the
# frontend uses same-origin requests.
NEXT_PUBLIC_API_BASE=http://localhost:8080
